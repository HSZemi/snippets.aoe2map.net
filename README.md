snippets.aoe2map.net TESTS
==========================

- setup a python virtual environment
- install requirements with `pip install -r requirements.txt`
- copy `config.py.template` to `config.py` and adjust url
- run tests